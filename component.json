{
  "title": "Sync by AVA",
  "description": "Sync extension to connect with Sync Mongo Storage Engine",
  "docsUrl": "https://github.com/thatapp/Sync",
  "buildType":"docker",
  "envVars": {
    "USERNAME": {
      "required": true,
      "description": "Your Mongo Username"
    },
    "PASSWORD": {
      "required": true,
      "description": "Your Mongo Password"
    }
  },
  "actions": {
    "upsert_old": {
      "main": "./lib/actions/upsert_old.js",
      "title": "Upsert data into Sync Mongo",
      "description": "Upsert data",
      "metadata": {
        "in": "./lib/schemas/upsert.in.json"
      }
    },
    "upsert_new": {
      "main": "./lib/actions/upsert_new.js",
      "title": "Upsert data into Sync Mongo",
      "description": "Upsert data",
      "metadata": {
        "in": "./lib/schemas/upsert.in.json"
      }
    },
    "aggregate_old": {
      "main": "./lib/actions/aggregate_old.js",
      "title": "Aggregation into Sync Mongo",
      "description": "Update data",
      "fields": {
        "behaviour": {
          "viewClass": "SelectView",
          "prompt": "Emit Behavior",
          "label": "Emit Behavior",
          "required": true,
          "model": {
            "individually": "Emit Individually",
            "fetch_all": "Fetch All"
          }
        }
      },
      "metadata": {
        "in": "./lib/schemas/aggregation.in.json"
      }
    },
    "aggregate_new": {
      "main": "./lib/actions/aggregate_new.js",
      "title": "Aggregation into Sync Mongo",
      "description": "Update data",
      "fields": {
        "behaviour": {
          "viewClass": "SelectView",
          "prompt": "Emit Behavior",
          "label": "Emit Behavior",
          "required": true,
          "model": {
            "individually": "Emit Individually",
            "fetch_all": "Fetch All"
          }
        }
      },
      "metadata": {
        "in": "./lib/schemas/aggregation.in.json"
      }
    }
  }
}
